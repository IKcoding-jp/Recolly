# UI改善 5項目 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** フロントエンドの5箇所のUI改善を一括で実施する（Issue #66）

**Architecture:** 各改善は独立した変更で、互いに依存しない。既存の `mediaTypeUtils.ts` のユーティリティ関数を活用して、メディアタイプ別の表示制御を実装する。バックエンド変更は不要。

**Tech Stack:** React 19 / TypeScript / Vitest + React Testing Library / CSS Modules

**Spec:** `docs/superpowers/specs/2026-03-28-ui-polish-design.md`

---

### Task 1: mediaTypeUtils に再視聴ラベル関数を追加

**Files:**
- Modify: `frontend/src/lib/mediaTypeUtils.ts`
- Modify: `frontend/src/lib/mediaTypeUtils.test.ts`

- [ ] **Step 1: テストを書く**

`frontend/src/lib/mediaTypeUtils.test.ts` の末尾に追加:

```typescript
describe('getRewatchLabel', () => {
  it('アニメは「再視聴回数」を返す', () => {
    expect(getRewatchLabel('anime')).toBe('再視聴回数')
  })
  it('映画は「再視聴回数」を返す', () => {
    expect(getRewatchLabel('movie')).toBe('再視聴回数')
  })
  it('ドラマは「再視聴回数」を返す', () => {
    expect(getRewatchLabel('drama')).toBe('再視聴回数')
  })
  it('本は「再読回数」を返す', () => {
    expect(getRewatchLabel('book')).toBe('再読回数')
  })
  it('漫画は「再読回数」を返す', () => {
    expect(getRewatchLabel('manga')).toBe('再読回数')
  })
  it('ゲームは「リプレイ回数」を返す', () => {
    expect(getRewatchLabel('game')).toBe('リプレイ回数')
  })
})
```

import文に `getRewatchLabel` を追加:
```typescript
import {
  getActionLabel,
  getProgressText,
  getGenreLabel,
  hasEpisodes,
  getStatusLabel,
  getStatusOptions,
  getRewatchLabel,
} from './mediaTypeUtils'
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd frontend && npx vitest run src/lib/mediaTypeUtils.test.ts`
Expected: FAIL — `getRewatchLabel` is not exported

- [ ] **Step 3: 実装する**

`frontend/src/lib/mediaTypeUtils.ts` に追加:

```typescript
// 再視聴/再読/リプレイのラベル
const REWATCH_LABELS: Record<'video' | 'reading' | 'game', string> = {
  video: '再視聴回数',
  reading: '再読回数',
  game: 'リプレイ回数',
}

/** メディアタイプに応じた再視聴ラベルを返す */
export function getRewatchLabel(mediaType: MediaType): string {
  return REWATCH_LABELS[MEDIA_TYPE_GROUP[mediaType]]
}
```

- [ ] **Step 4: テストがパスすることを確認**

Run: `cd frontend && npx vitest run src/lib/mediaTypeUtils.test.ts`
Expected: ALL PASS

- [ ] **Step 5: コミット**

```bash
git add frontend/src/lib/mediaTypeUtils.ts frontend/src/lib/mediaTypeUtils.test.ts
git commit -m "feat: mediaTypeUtilsに再視聴ラベル関数を追加 (#66)"
```

---

### Task 2: ProgressControl にメディアタイプ対応を追加

**Files:**
- Modify: `frontend/src/components/ui/ProgressControl/ProgressControl.tsx`
- Modify: `frontend/src/components/ui/ProgressControl/ProgressControl.test.tsx`

- [ ] **Step 1: テストを書く**

`frontend/src/components/ui/ProgressControl/ProgressControl.test.tsx` に追加:

```typescript
  it('漫画の場合は「巻」を表示する', () => {
    render(<ProgressControl current={5} total={20} onChange={() => {}} mediaType="manga" />)
    expect(screen.getByText('5 / 20巻')).toBeInTheDocument()
  })

  it('漫画でtotalがnullの場合は「5巻」を表示する', () => {
    render(<ProgressControl current={5} total={null} onChange={() => {}} mediaType="manga" />)
    expect(screen.getByText('5巻')).toBeInTheDocument()
  })

  it('mediaType未指定の場合はデフォルトで「話」を表示する', () => {
    render(<ProgressControl current={5} total={24} onChange={() => {}} />)
    expect(screen.getByText('5 / 24話')).toBeInTheDocument()
  })
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd frontend && npx vitest run src/components/ui/ProgressControl/ProgressControl.test.tsx`
Expected: FAIL — `mediaType` prop is unknown / 「巻」が見つからない

- [ ] **Step 3: 実装する**

`frontend/src/components/ui/ProgressControl/ProgressControl.tsx` を以下のように変更:

```typescript
import type { ChangeEvent } from 'react'
import type { MediaType } from '../../../lib/types'
import styles from './ProgressControl.module.css'

// 話数・巻数の単位ラベル（mediaTypeUtils.tsと同じ定義だが、
// このコンポーネントは軽量に保つため直接定義）
const UNIT_LABELS: Partial<Record<MediaType, string>> = {
  anime: '話',
  drama: '話',
  manga: '巻',
}

type ProgressControlProps = {
  current: number
  total: number | null
  onChange: (episode: number) => void
  showFullControls?: boolean
  mediaType?: MediaType
}

export function ProgressControl({
  current,
  total,
  onChange,
  showFullControls = false,
  mediaType,
}: ProgressControlProps) {
  const canIncrement = total === null || current < total
  const canDecrement = current > 0
  const percentage = total ? Math.round((current / total) * 100) : null
  const unit = (mediaType && UNIT_LABELS[mediaType]) ?? '話'

  const handleInputChange = (e: ChangeEvent<HTMLInputElement>) => {
    const value = parseInt(e.target.value, 10)
    if (!isNaN(value) && value >= 0 && (total === null || value <= total)) {
      onChange(value)
    }
  }

  return (
    <div className={styles.container}>
      <div className={styles.controls}>
        {showFullControls && (
          <button
            type="button"
            className={styles.button}
            onClick={() => onChange(current - 1)}
            disabled={!canDecrement}
            aria-label="-1"
          >
            -
          </button>
        )}
        <span className={styles.display}>
          {total !== null ? `${current} / ${total}${unit}` : `${current}${unit}`}
        </span>
        <button
          type="button"
          className={`${styles.button} ${styles.increment}`}
          onClick={() => onChange(current + 1)}
          disabled={!canIncrement}
          aria-label="+1"
        >
          +
        </button>
        {showFullControls && (
          <input
            type="number"
            className={styles.input}
            value={current}
            onChange={handleInputChange}
            min={0}
            max={total ?? undefined}
            aria-label="話数入力"
          />
        )}
      </div>
      {total !== null && percentage !== null && (
        <div className={styles.bar}>
          <div className={styles.fill} style={{ width: `${percentage}%` }} />
        </div>
      )}
    </div>
  )
}
```

- [ ] **Step 4: テストがパスすることを確認**

Run: `cd frontend && npx vitest run src/components/ui/ProgressControl/ProgressControl.test.tsx`
Expected: ALL PASS

- [ ] **Step 5: コミット**

```bash
git add frontend/src/components/ui/ProgressControl/ProgressControl.tsx frontend/src/components/ui/ProgressControl/ProgressControl.test.tsx
git commit -m "feat: ProgressControlにmediaTypeプロップを追加し単位を動的に変更 (#66)"
```

---

### Task 3: WorkDetailPage — 進捗セクションの条件表示と再視聴ラベル最適化

**Files:**
- Modify: `frontend/src/pages/WorkDetailPage/WorkDetailPage.tsx`
- Modify: `frontend/src/pages/WorkDetailPage/WorkDetailPage.test.tsx`

- [ ] **Step 1: テストを書く**

`frontend/src/pages/WorkDetailPage/WorkDetailPage.test.tsx` に追加:

```typescript
  it('アニメの場合は進捗セクションが表示される', async () => {
    renderWithRouter('1')
    await waitFor(() => {
      expect(screen.getByText('進捗')).toBeInTheDocument()
    })
  })

  it('映画の場合は進捗セクションが非表示', async () => {
    vi.mocked(recordsApi.getAll).mockResolvedValue({
      records: [{ ...mockRecord, work: { ...mockRecord.work, media_type: 'movie' as const, total_episodes: null } }],
    })
    renderWithRouter('1')
    await waitFor(() => {
      expect(screen.getByText('進撃の巨人')).toBeInTheDocument()
    })
    expect(screen.queryByText('進捗')).not.toBeInTheDocument()
  })

  it('ゲームの場合は進捗セクションが非表示', async () => {
    vi.mocked(recordsApi.getAll).mockResolvedValue({
      records: [{ ...mockRecord, work: { ...mockRecord.work, media_type: 'game' as const, total_episodes: null } }],
    })
    renderWithRouter('1')
    await waitFor(() => {
      expect(screen.getByText('進撃の巨人')).toBeInTheDocument()
    })
    expect(screen.queryByText('進捗')).not.toBeInTheDocument()
  })

  it('アニメの場合は「再視聴回数」ラベルが表示される', async () => {
    renderWithRouter('1')
    await waitFor(() => {
      expect(screen.getByText('再視聴回数')).toBeInTheDocument()
    })
  })

  it('ゲームの場合は「リプレイ回数」ラベルが表示される', async () => {
    vi.mocked(recordsApi.getAll).mockResolvedValue({
      records: [{ ...mockRecord, work: { ...mockRecord.work, media_type: 'game' as const, total_episodes: null } }],
    })
    renderWithRouter('1')
    await waitFor(() => {
      expect(screen.getByText('リプレイ回数')).toBeInTheDocument()
    })
  })

  it('本の場合は「再読回数」ラベルが表示される', async () => {
    vi.mocked(recordsApi.getAll).mockResolvedValue({
      records: [{ ...mockRecord, work: { ...mockRecord.work, media_type: 'book' as const, total_episodes: null } }],
    })
    renderWithRouter('1')
    await waitFor(() => {
      expect(screen.getByText('再読回数')).toBeInTheDocument()
    })
  })
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd frontend && npx vitest run src/pages/WorkDetailPage/WorkDetailPage.test.tsx`
Expected: FAIL — 映画で「進捗」が表示される / ゲームで「リプレイ回数」が表示されない

- [ ] **Step 3: 実装する**

`frontend/src/pages/WorkDetailPage/WorkDetailPage.tsx` を修正:

import に `hasEpisodes` と `getRewatchLabel` を追加:
```typescript
import { hasEpisodes, getRewatchLabel } from '../../lib/mediaTypeUtils'
```

進捗セクション（102-110行目）を条件付き表示に変更:
```typescript
            {hasEpisodes(work.media_type) && (
              <div className={styles.section}>
                <div className={styles.label}>進捗</div>
                <ProgressControl
                  current={record.current_episode}
                  total={work.total_episodes}
                  onChange={handleEpisodeChange}
                  showFullControls
                  mediaType={work.media_type}
                />
              </div>
            )}
```

再視聴セクション（112-115行目）のラベルを動的に変更:
```typescript
            <div className={styles.section}>
              <div className={styles.label}>{getRewatchLabel(work.media_type)}</div>
              <RewatchControl count={record.rewatch_count} onChange={handleRewatchCountChange} />
            </div>
```

- [ ] **Step 4: テストがパスすることを確認**

Run: `cd frontend && npx vitest run src/pages/WorkDetailPage/WorkDetailPage.test.tsx`
Expected: ALL PASS

- [ ] **Step 5: コミット**

```bash
git add frontend/src/pages/WorkDetailPage/WorkDetailPage.tsx frontend/src/pages/WorkDetailPage/WorkDetailPage.test.tsx
git commit -m "feat: 単発作品で進捗セクションを非表示、再視聴ラベルをメディアタイプ別に最適化 (#66)"
```

---

### Task 4: ヘッダー — ナビから「マイページ」を削除、ドロップダウン順序入替

**Files:**
- Modify: `frontend/src/components/ui/NavBar/NavBar.tsx`
- Modify: `frontend/src/components/ui/NavBar/NavBar.test.tsx`
- Modify: `frontend/src/components/ui/UserMenu/UserMenu.tsx`
- Modify: `frontend/src/components/ui/UserMenu/UserMenu.test.tsx`

- [ ] **Step 1: NavBar のテストを修正**

`frontend/src/components/ui/NavBar/NavBar.test.tsx` の 37-43行目を変更:

既存テスト「未実装のナビ項目はリンクではなくグレーアウトで表示する」を修正:
```typescript
  it('未実装のナビ項目はリンクではなくグレーアウトで表示する', () => {
    renderNavBar()
    expect(screen.getByText('コミュニティ')).toBeInTheDocument()
    expect(screen.getByText('おすすめ')).toBeInTheDocument()
    expect(screen.queryByRole('link', { name: 'コミュニティ' })).not.toBeInTheDocument()
  })

  it('ナビに「マイページ」リンクが表示されない', () => {
    renderNavBar()
    // NavBar内にはマイページのリンクは存在しない（UserMenuのドロップダウンのみ）
    const navLinks = screen.getAllByRole('link')
    const mypageLink = navLinks.find(link => link.textContent === 'マイページ')
    expect(mypageLink).toBeUndefined()
  })
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd frontend && npx vitest run src/components/ui/NavBar/NavBar.test.tsx`
Expected: FAIL — 「マイページ」リンクがまだナビに存在する

- [ ] **Step 3: NavBar を実装する**

`frontend/src/components/ui/NavBar/NavBar.tsx` の `NAV_ITEMS` から「マイページ」を削除:

```typescript
const NAV_ITEMS: NavItem[] = [
  { label: 'ホーム', path: '/dashboard' },
  { label: '検索', path: '/search' },
  { label: 'ライブラリ', path: '/library' },
  { label: 'コミュニティ', path: null },
  { label: 'おすすめ', path: null },
]
```

- [ ] **Step 4: NavBar テストがパスすることを確認**

Run: `cd frontend && npx vitest run src/components/ui/NavBar/NavBar.test.tsx`
Expected: ALL PASS

- [ ] **Step 5: UserMenu のテストを修正**

`frontend/src/components/ui/UserMenu/UserMenu.test.tsx` のドロップダウン表示テスト（34-41行目）を修正。「マイページ」が「設定」より先に表示されることを確認:

```typescript
  it('クリックでドロップダウンを表示する（マイページ→設定の順）', async () => {
    const user = userEvent.setup()
    renderMenu()
    await user.click(screen.getByRole('button', { name: 'ユーザーメニュー' }))
    expect(screen.getByText('ik@example.com')).toBeInTheDocument()
    expect(screen.getByText('ログアウト')).toBeInTheDocument()
    expect(screen.getByRole('link', { name: 'マイページ' })).toBeInTheDocument()
    expect(screen.getByRole('link', { name: '設定' })).toBeInTheDocument()

    // マイページが設定より先に表示される（DOM順序で確認）
    const links = screen.getAllByRole('link')
    const mypageIndex = links.findIndex(l => l.textContent === 'マイページ')
    const settingsIndex = links.findIndex(l => l.textContent === '設定')
    expect(mypageIndex).toBeLessThan(settingsIndex)
  })
```

- [ ] **Step 6: UserMenu テストが失敗することを確認**

Run: `cd frontend && npx vitest run src/components/ui/UserMenu/UserMenu.test.tsx`
Expected: FAIL — マイページが設定の後に表示される

- [ ] **Step 7: UserMenu を実装する**

`frontend/src/components/ui/UserMenu/UserMenu.tsx` のドロップダウン内リンク順序を変更（52-58行目）:

```typescript
          <Link to="/mypage" className={styles.item} onClick={() => setIsOpen(false)}>
            マイページ
          </Link>
          <Link to="/settings" className={styles.item} onClick={() => setIsOpen(false)}>
            設定
          </Link>
```

- [ ] **Step 8: UserMenu テストがパスすることを確認**

Run: `cd frontend && npx vitest run src/components/ui/UserMenu/UserMenu.test.tsx`
Expected: ALL PASS

- [ ] **Step 9: コミット**

```bash
git add frontend/src/components/ui/NavBar/NavBar.tsx frontend/src/components/ui/NavBar/NavBar.test.tsx frontend/src/components/ui/UserMenu/UserMenu.tsx frontend/src/components/ui/UserMenu/UserMenu.test.tsx
git commit -m "feat: ナビからマイページを削除しドロップダウンのみに、順番を入替 (#66)"
```

---

### Task 5: WorkCard — 「記録する」ボタンを右端に分離

**Files:**
- Modify: `frontend/src/components/WorkCard/WorkCard.tsx`
- Modify: `frontend/src/components/WorkCard/WorkCard.module.css`
- Modify: `frontend/src/components/WorkCard/WorkCard.test.tsx`

- [ ] **Step 1: テストを書く**

`frontend/src/components/WorkCard/WorkCard.test.tsx` に追加:

```typescript
  it('「記録する」ボタンがinfo領域の外に配置されている', () => {
    const { container } = render(<WorkCard work={mockWork} onRecord={vi.fn()} />)
    const action = container.querySelector('[class*="action"]')
    expect(action).toBeInTheDocument()
    expect(action?.querySelector('button')).toBeInTheDocument()
  })

  it('記録済みの場合は「記録済み」がaction領域に表示される', () => {
    const { container } = render(<WorkCard work={mockWork} onRecord={vi.fn()} isRecorded />)
    const action = container.querySelector('[class*="action"]')
    expect(action?.textContent).toBe('記録済み')
  })
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd frontend && npx vitest run src/components/WorkCard/WorkCard.test.tsx`
Expected: FAIL — `action` クラスが見つからない

- [ ] **Step 3: WorkCard.tsx を実装する**

`frontend/src/components/WorkCard/WorkCard.tsx` のJSXを変更:

```typescript
  return (
    <div className={styles.card}>
      <div className={styles.coverWrapper}>
        {work.cover_image_url ? (
          <img
            className={styles.cover}
            src={work.cover_image_url}
            alt={`${work.title}のカバー画像`}
          />
        ) : (
          <div className={styles.coverPlaceholder} />
        )}
      </div>
      <div className={styles.info}>
        <span className={`${styles.genre} ${styles[work.media_type]}`}>
          {MEDIA_TYPE_LABELS[work.media_type]}
        </span>
        <h3 className={styles.title}>{work.title}</h3>
        {work.description && <p className={styles.description}>{work.description}</p>}
      </div>
      <div className={styles.action}>
        {isRecorded ? (
          <span className={styles.recorded}>記録済み</span>
        ) : (
          <Button variant="primary" size="sm" onClick={handleRecord} disabled={isLoading}>
            {isLoading ? '記録中...' : '記録する'}
          </Button>
        )}
      </div>
    </div>
  )
```

- [ ] **Step 4: WorkCard.module.css を実装する**

`.card` に `align-items: flex-start` を追加し、`.action` クラスを追加:

```css
.card {
  display: flex;
  gap: var(--spacing-md);
  padding: var(--spacing-md);
  border-bottom: var(--border-width) var(--border-style) var(--color-border-light);
  align-items: flex-start;
}

/* 既存の .info はそのまま。flex: 1 と min-width: 0 を追加 */
.info {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-xs);
  min-width: 0;
  flex: 1;
}

/* 新規: ボタン/記録済みテキストの配置 */
.action {
  flex-shrink: 0;
  padding-top: var(--spacing-xs);
}
```

- [ ] **Step 5: テストがパスすることを確認**

Run: `cd frontend && npx vitest run src/components/WorkCard/WorkCard.test.tsx`
Expected: ALL PASS

- [ ] **Step 6: コミット**

```bash
git add frontend/src/components/WorkCard/WorkCard.tsx frontend/src/components/WorkCard/WorkCard.module.css frontend/src/components/WorkCard/WorkCard.test.tsx
git commit -m "feat: 検索結果カードの記録ボタンを右端に分離配置 (#66)"
```

---

### Task 6: 検索ページ — 手動登録セクションのデザイン改善

**Files:**
- Modify: `frontend/src/pages/SearchPage/SearchPage.tsx`
- Modify: `frontend/src/pages/SearchPage/SearchPage.module.css`

- [ ] **Step 1: SearchPage.tsx を修正する**

`frontend/src/pages/SearchPage/SearchPage.tsx` の手動登録セクション（178-184行目）を変更:

```typescript
        <div className={styles.manualSection}>
          <div className={styles.manualCard}>
            <p className={styles.manualGuide}>お探しの作品が見つかりませんか？</p>
            <Button variant="secondary" onClick={() => setShowManualForm(!showManualForm)}>
              {showManualForm ? '閉じる' : '+ 手動で登録する'}
            </Button>
          </div>
          {showManualForm && <ManualWorkForm onSubmit={handleManualSubmit} />}
        </div>
```

import文から `SectionTitle` を削除（他で使っていなければ）。

- [ ] **Step 2: SearchPage.module.css を修正する**

`.manualSection` のスタイルを変更し、`.manualCard` と `.manualGuide` を追加:

```css
.manualSection {
  margin-top: var(--spacing-2xl);
}

.manualCard {
  border: 1px dashed var(--color-border-light);
  border-radius: 8px;
  padding: var(--spacing-xl);
  text-align: center;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: var(--spacing-sm);
}

.manualGuide {
  font-family: var(--font-body);
  font-size: var(--font-size-label);
  color: var(--color-text-muted);
  margin: 0;
}
```

- [ ] **Step 3: テストがパスすることを確認**

Run: `cd frontend && npx vitest run src/pages/SearchPage/SearchPage.test.tsx`
Expected: ALL PASS（既存テストが壊れていないことを確認）

- [ ] **Step 4: コミット**

```bash
git add frontend/src/pages/SearchPage/SearchPage.tsx frontend/src/pages/SearchPage/SearchPage.module.css
git commit -m "feat: 検索ページの手動登録セクションをガイド付き破線カードに改善 (#66)"
```

---

### Task 7: マイライブラリ — 空状態の絵文字をSVGアイコンに変更

**Files:**
- Modify: `frontend/src/pages/LibraryPage/LibraryPage.tsx`
- Modify: `frontend/src/pages/LibraryPage/LibraryPage.test.tsx`

- [ ] **Step 1: テストを修正する**

`frontend/src/pages/LibraryPage/LibraryPage.test.tsx` で絵文字の存在を確認するテストがあれば修正。なければ新規追加:

```typescript
  it('空状態でSVGアイコンが表示される（絵文字ではない）', async () => {
    // 空の records を返すようにモック設定
    // ... 既存のセットアップに合わせる
    const { container } = renderLibraryPage() // 既存のレンダーヘルパー
    await waitFor(() => {
      expect(screen.getByText('作品を探して記録しましょう')).toBeInTheDocument()
    })
    // SVGアイコンが表示される
    const svg = container.querySelector('svg')
    expect(svg).toBeInTheDocument()
    // 絵文字が表示されない
    expect(screen.queryByText('📚')).not.toBeInTheDocument()
  })
```

注意: 既存テストのモック設定・レンダーヘルパーに合わせて調整すること。

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd frontend && npx vitest run src/pages/LibraryPage/LibraryPage.test.tsx`
Expected: FAIL — 📚がまだ表示される / SVGが見つからない

- [ ] **Step 3: LibraryPage.tsx を修正する**

`frontend/src/pages/LibraryPage/LibraryPage.tsx` の空状態セクション（114-115行目）を変更:

```typescript
            <div className={styles.emptyGuide}>
              <svg
                className={styles.emptyIcon}
                width="48"
                height="48"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                strokeWidth="1.5"
                strokeLinecap="round"
                strokeLinejoin="round"
                aria-hidden="true"
              >
                <path d="M4 19.5v-15A2.5 2.5 0 0 1 6.5 2H20v20H6.5a2.5 2.5 0 0 1 0-5H20" />
                <path d="M8 7h6" />
                <path d="M8 11h4" />
              </svg>
              <p className={styles.emptyTitle}>作品を探して記録しましょう</p>
              <Button variant="primary" onClick={handleGoToSearch}>
                作品を検索する
              </Button>
            </div>
```

`.emptyIcon` のCSSは既存の `p.emptyIcon` を変更:
```css
.emptyIcon {
  color: var(--color-text-muted);
  margin-bottom: var(--spacing-sm);
}
```

- [ ] **Step 4: テストがパスすることを確認**

Run: `cd frontend && npx vitest run src/pages/LibraryPage/LibraryPage.test.tsx`
Expected: ALL PASS

- [ ] **Step 5: コミット**

```bash
git add frontend/src/pages/LibraryPage/LibraryPage.tsx frontend/src/pages/LibraryPage/LibraryPage.test.tsx
git commit -m "feat: ライブラリ空状態の絵文字をSVGアイコンに変更 (#66)"
```

---

### Task 8: 全テスト・リンター実行

- [ ] **Step 1: 全テストを実行**

Run: `cd frontend && npx vitest run`
Expected: ALL PASS

- [ ] **Step 2: ESLint を実行**

Run: `cd frontend && npx eslint src/`
Expected: エラーなし

- [ ] **Step 3: 問題があれば修正してコミット**
